<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--Themes-->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:statusBarColor">@color/status_bar</item>
    </style>

    <style name="AppTheme.BackgroundStyle">
        <item name="android:background">@color/primaryLight</item>
    </style>

    <style name="AppTheme.FloatingActionButton" parent="@style/Widget.MaterialComponents.FloatingActionButton">
        <item name="tint">@color/primary</item>
        <item name="backgroundTint">@color/accent</item>
    </style>

    <!-- ToolBar -->
    <style name="ToolBarStyle" parent="Theme.AppCompat">
        <item name="android:background">@color/toolbar</item>
        <item name="android:textColorPrimary">@color/toolbar_text_primary</item>
        <item name="android:textColorSecondary">@color/toolbar_text_secondary</item>
        <item name="actionMenuTextColor">@color/toolbar_menu</item>
    </style>
</resources>
